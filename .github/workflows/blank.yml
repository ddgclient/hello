# This is a basic workflow to help you get started with Actions
name: CI

on: 
  push:

jobs:
  testenv:
    runs-on: self-hosted
    steps:
      - name: testrun
        run: powershell "dir env:"
      - name: restore
        run: "dotnet tool restore"
      - name: compile
        # run:  "${{ format('{0}/pipeline_scripts/CompileAll.cmd', github.workspace) }}"
        run: sh ./pipeline_scripts/CompileAll.cmd
          
